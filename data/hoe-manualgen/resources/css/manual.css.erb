/* 
 *	<%= name %> Manual Stylesheet
 *	Authors:
 *  - Michael Granger <ged@FaerieMUD.org>
 */

@import url(baseline.compress.css);

#page {
	margin: 0 auto;
}

#page > header {
	position: relative;
	background-color: #e6eef7;
	background: -webkit-gradient(radial, 
		20% 0, 0, 
		20% 0, 800, 
		color-stop(0.0,#e6eef7), 
		color-stop(1.0,#a6aeb7));
	background: -moz-radial-gradient(
		20% top, 
		farthest-side, 
		#c6ced7, 
		#a6aeb7);
	color: #fff;
	padding: 0.4em 1em;
}

#page > header hgroup h1,
#page > header hgroup h2 {
	margin: 0;
	text-shadow: ;
}

#page > header h1 {
	font-size: 4em;
	text-shadow: 1px 1px 2px rgba( 25,25,25, 0.5 );
}
#page > header h2 {
	font-size: 2em;
	text-shadow: 1px 1px 2px rgba( 25,25,25, 0.5 );
}

header a {
	color: inherit;
	text-decoration: inherit;
}

#page > header nav {
	position: absolute;
	top: 4px;	
	right: 1em;
}

footer {
	clear: both;
}